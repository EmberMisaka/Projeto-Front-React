
  # Marketplace Web Application

  This is a code bundle for Marketplace Web Application. The original project is available at https://www.figma.com/design/5G2asHD14Whf4dZ9FY7Xef/Marketplace-Web-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm install -g create-vite` to install vite

  Run `npm run dev` to start the development server.
  
